import React from 'react';
import {usePartnerContext}  from './PartnerContextApi/PartnerContext';

const PartnerNavBar = () => {
  const { fine } = usePartnerContext();
  return (
    <nav className="bg-white shadow p-4 flex justify-between items-center font-poppins">
      
      
    </nav>
  );
};

export default PartnerNavBar;
